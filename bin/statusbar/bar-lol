#!/bin/sh
case $BUTTON in
	1) sysctl -w abi.vsyscall32=0 ;; 
	4) st -e vim /usr/local/bin/bar-lol;;
esac

Abi=$(sysctl abi.vsyscall32 | awk -F '=' '{ print $2 }')
if [[ $Abi == '0' ]]; then
	echo "[Lol: Ok]"
else
	echo "[Lol: No]"
fi
